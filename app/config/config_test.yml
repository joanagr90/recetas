imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

#TEMA 10 - Configuración tests
#PAra que en el entorno test no se envíen realmente los emails.
swiftmailer:
    disable_delivery: true
